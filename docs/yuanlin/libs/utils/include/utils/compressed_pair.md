# compressed_pair.h

## 文件概述
空基类优化 (EBO) 的压缩对实现，通过私有继承减少空类型的存储开销。

## 核心类/结构体/函数
- **compressed_pair<T1, T2>**: 压缩对模板类
  - `first()` / `second()`: 访问两个元素
  - `swap()`: 交换两个压缩对的内容

## 关键实现逻辑
- 通过私有继承 T1 和 T2 实现空基类优化
- 当 T1 或 T2 为空类时，不占用额外存储空间
- 要求 T1 != T2（类型必须不同）

## 依赖关系
- 无外部 utils 依赖
