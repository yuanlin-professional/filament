# Zip2Iterator.h

## 文件概述
双迭代器打包工具，将两个随机访问迭代器合并为一个，可用于关联排序。

## 核心类/结构体/函数
- **Zip2Iterator<It1, It2>**: 打包迭代器
  - 解引用返回 `pair<Ref1, Ref2>`
  - 支持完整的随机访问迭代器接口
  - 自定义 swap 保证两个数组同步交换

## 关键实现逻辑
- 主要用例：按一个数组的值排序时同步另一个数组
- Ref 类型支持自定义 swap，使 std::sort 等算法能正确工作

## 依赖关系
- 无外部 utils 依赖
