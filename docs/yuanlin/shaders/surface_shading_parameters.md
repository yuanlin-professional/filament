# surface_shading_parameters.fs

## 文件概述
着色参数初始化。在片段着色器中计算并填充全局着色变量（位置、法线、视线方向、TBN 矩阵等）。

## 核心功能
- 从 varying 插值数据计算世界空间位置和视线方向
- 构建和变换法线（包括法线贴图应用）
- 计算 NoV（法线与视线的点积），钳制到最小值避免除零
- 处理弯曲法线和透明涂层法线

## 依赖关系
- `common_shading.fs` - 全局着色变量声明
- `surface_getters.fs` - 获取插值属性
